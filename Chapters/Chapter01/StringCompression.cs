using System;

namespace Chapters.Chapter01
{
    /// <summary>
    /// Implement a method to perform basic string compression using the counts of repeated characters.
    /// For example, the string aabccccaaa would become a2bc5a3. If the "compressed" string would not become
    /// smaller then the original string, your method should return the original string.
    /// You can assume the string has only uppercase and lowercase letters (a-z).
    /// </summary>
    public static class StringCompression
    {
        public static string Compress(this string str)
        {
            throw new NotImplementedException("Compress");
        }
    }
}