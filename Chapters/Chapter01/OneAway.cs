using System;

namespace Chapters.Chapter01
{
    /// <summary>
    /// There are three types og edits that can be performed on strings: insert a character, remove a character,
    /// or replace a character. Given two strings, write a function to check if they are one edit (or zero edits) away.
    /// </summary>
    public static class OneAway
    {
        public static bool IsOneAwayFrom(this string a, string b)
        {
            throw new NotImplementedException();
        }
    }
}